# Function for Dirichlet parameters

Function to map a confidence value to a parameterisation of a Dirichlet
distribution.

## Usage

``` r
dirichlet_pointiness(confidence)
```

## Arguments

- confidence:

  value between 0 and 1

## Value

parameterisation

## Details

Note that the parameterisation is somewhat arbitrary but seems to work
on visual inspection.
